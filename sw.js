let GHPATH = "/radio";
let APP_PREFIX = "de.lclutz"
var VERSION = 'version_00';
let URLS = [
  `${GHPATH}/`,
  `${GHPATH}/index.html`,
  `${GHPATH}/styles.css`,
  `${GHPATH}/app.js`,
  `${GHPATH}/stations.json`,
  `${GHPATH}/images`
]
